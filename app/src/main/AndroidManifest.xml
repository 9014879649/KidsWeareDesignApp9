<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.trendyboys.kidsdresses">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"></uses-permission>

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"

        android:supportsRtl="true"
        android:theme="@style/AppTheme">


        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-8~3565939254999972485"/>

        <activity android:name="com.trendyboys.kidsdresses.FirstScreenActivity"
            android:configChanges="orientation"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.trendyboys.kidsdresses.MainActivity"
            android:configChanges="orientation"
            />
        <activity android:name="com.trendyboys.kidsdresses.SelectedItemsActivity"
            android:launchMode = "singleTop"
            android:configChanges="orientation"
            />
        <activity android:name="com.trendyboys.kidsdresses.BigImageActivity"
            android:launchMode = "singleTop"
            android:configChanges="orientation"
            ></activity>
    </application>

</manifest>
